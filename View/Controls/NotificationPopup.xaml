<UserControl x:Class="ReturnToStonks.View.Controls.NotificationPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReturnToStonks.View.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid>
    <Border Background="LightGray" CornerRadius="10" Padding="10" Opacity="{Binding NotificationOpacity}">
      <TextBlock Text="{Binding NotificationMessage}" VerticalAlignment="Center" HorizontalAlignment="Center" 
                 FontFamily="Arial" FontSize="15" Opacity="1"/>
    </Border>
  </Grid>
</UserControl>
